# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# Public base URL used for Location headers on POSTs.
# In dev you can omit this and we’ll fall back to http://localhost:PORT
# On EC2 set this to: http://<your-ec2-public-dns>:8080
API_URL=

# Auth (A1 dev/tests use Basic via Passport)
# For A1, no secret needed; Cognito is for EC2 demo later.
# When you wire Cognito, add your pool & region here.
COGNITO_POOL_ID=
AWS_REGION=
